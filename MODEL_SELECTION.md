# Claude Model Selection Guide

## 🎯 모델 선택 전략

### Claude 3 Sonnet 사용 (빠르고 효율적)
토큰 소비: 낮음 | 속도: 빠름 | 비용: 저렴

#### ✅ Sonnet 적합 작업
```
1. UI 구현
   - Composable 함수 작성
   - 레이아웃 구성
   - 스타일링
   - 애니메이션

2. 기본 CRUD 작업
   - Repository 메서드
   - DAO 쿼리
   - 간단한 비즈니스 로직

3. 테스트 코드
   - 단위 테스트
   - UI 테스트
   - Mock 객체 생성

4. 버그 수정
   - 간단한 로직 오류
   - UI 버그
   - 타입 오류

5. 코드 리뷰
   - 스타일 개선
   - 성능 최적화
   - 리팩토링 제안

6. 문서화
   - 주석 추가
   - README 업데이트
   - API 문서
```

#### 💡 Sonnet 사용 예시
```bash
# UI 컴포넌트 생성
claude-code --model sonnet "Create ChatBubble composable with sender/receiver styles"

# 간단한 함수 구현
claude-code --model sonnet "Add validation to message input field"

# 테스트 작성
claude-code --model sonnet "Write unit tests for ChatViewModel"
```

---

### Claude 3 Opus 사용 (강력하고 정밀함)
토큰 소비: 높음 | 속도: 보통 | 비용: 비쌈

#### ✅ Opus 적합 작업
```
1. 아키텍처 설계
   - 전체 시스템 설계
   - 모듈 간 통신
   - 디자인 패턴 적용
   - 확장성 고려

2. 복잡한 비즈니스 로직
   - AI 프롬프트 엔지니어링
   - 알고리즘 구현
   - 상태 관리 시스템
   - 동시성 처리

3. 통합 작업
   - 여러 모듈 연결
   - API 통합
   - 데이터 마이그레이션
   - 시스템 리팩토링

4. 성능 최적화
   - 메모리 최적화
   - 알고리즘 개선
   - 캐싱 전략
   - 배터리 최적화

5. 보안 구현
   - 암호화
   - 인증/인가
   - 데이터 보호

6. 복잡한 디버깅
   - 메모리 누수
   - 동시성 문제
   - 성능 병목
```

#### 💡 Opus 사용 예시
```bash
# 아키텍처 설계
claude-code --model opus "Design conversation context management system with memory optimization"

# 복잡한 기능 구현
claude-code --model opus "Implement spaced repetition algorithm for vocabulary learning"

# 시스템 통합
claude-code --model opus "Integrate Gemini streaming API with error recovery and retry logic"
```

---

## 📊 작업별 모델 선택 매트릭스

| 작업 유형 | Sonnet | Opus | 이유 |
|---------|--------|------|------|
| 새 화면 생성 | ✅ | ❌ | 표준 패턴 따름 |
| ViewModel 작성 | ✅ | ❌ | 보일러플레이트 코드 |
| 복잡한 상태 관리 | ❌ | ✅ | 정밀한 설계 필요 |
| API 클라이언트 | ✅ | ❌ | Retrofit 표준 패턴 |
| 프롬프트 엔지니어링 | ❌ | ✅ | 미묘한 조정 필요 |
| DB 스키마 설계 | ❌ | ✅ | 장기적 영향 |
| 간단한 버그 수정 | ✅ | ❌ | 빠른 수정 |
| 메모리 누수 해결 | ❌ | ✅ | 깊은 분석 필요 |
| UI 애니메이션 | ✅ | ❌ | Compose 표준 |
| 학습 알고리즘 | ❌ | ✅ | 복잡한 로직 |

---

## 🔄 하이브리드 전략

### 단계적 접근법
```
1. 초기 설계 (Opus)
   → 아키텍처, 인터페이스 정의

2. 구현 (Sonnet)
   → 실제 코드 작성

3. 최적화 (Opus)
   → 성능 개선, 리팩토링

4. 테스트 (Sonnet)
   → 테스트 코드 생성

5. 디버깅 (상황에 따라)
   → 간단: Sonnet
   → 복잡: Opus
```

### 토큰 예산 관리
```python
일일 토큰 예산: 100,000
- Opus: 20,000 (20%)
- Sonnet: 80,000 (80%)

if 토큰_사용량 > 80,000:
    use_only_sonnet()
elif 토큰_사용량 > 50,000:
    prefer_sonnet()
else:
    use_best_model_for_task()
```

---

## 💰 비용 최적화 팁

1. **배치 작업**
   - 관련 작업을 모아서 한 번에 처리
   - 컨텍스트 재사용

2. **템플릿 활용**
   - 반복 패턴은 템플릿화
   - Sonnet으로 템플릿 적용

3. **점진적 개선**
   - MVP는 Sonnet으로
   - 중요 부분만 Opus로 개선

4. **캐싱**
   - 생성된 코드 재사용
   - 패턴 라이브러리 구축

5. **자동화**
   ```bash
   # .claude/auto_model_select.sh
   case "$1" in
     *UI*|*Component*|*Test*)
       echo "sonnet";;
     *Architecture*|*Algorithm*|*Complex*)
       echo "opus";;
     *)
       echo "sonnet";;  # 기본값
   esac
   ```